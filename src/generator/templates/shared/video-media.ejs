<% if (srcURL.endsWith('.gif')) { %>
<img src="<%= srcURL %>" class="product-main event-module-image" />
<% } else { %>
<video data-src="<%= srcURL %>" autoplay loop muted controls="false" class="product-main event-module-video"></video>
<% } %>
